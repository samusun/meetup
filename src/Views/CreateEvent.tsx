import React, { ReactElement } from 'react'

export default function CreateEvent(): ReactElement {
    return (
        <div>
            <button>Here you can create a new event</button>
        </div>
    )
}
